# -*- coding: utf-8 -*-
# UTF-8 encoding when using korean
N, K = map(int, input().split())
lst = list(map(int, input().split()))

n = 0
for i in range(N):
	if not str(K) in str(lst[i]):
		n += 1
		
print(n)